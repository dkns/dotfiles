#!/bin/zsh

if [ $(hostname) = 'serenity' ]; then
    xrandr --output DVI-D-1 --left-of HDMI-2 --output DP-2 --right-of HDMI-2 --output HDMI-2 --primary
fi

if [ $(hostname) = 'rp' ]; then
    xrandr --output HDMI-0 --rotate left --left-of DVI-D-0 --output VGA-0 --rotate left --right-of DVI-D-0
fi
