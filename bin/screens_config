#!/bin/zsh

if [ $(hostname) = 'serenity' ]; then
    xrandr --output DVI-D-1 --left-of HDMI-2 --output DP-2 --right-of HDMI-2 --output HDMI-2 --primary
fi

if [ $(hostname) = 'rp' ]; then
    xrandr --output DVI-D-0 --rotate left --left-of HDMI-0 --output VGA-0 --rotate left --right-of HDMI-0
fi
